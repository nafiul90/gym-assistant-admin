const useFilterItems = () => {
    const filterItems = [
        {
            label: "Site Title",
            name: "siteTitle",
            className: "col-span-12 lg:col-span-4"
        }
    ];

    return filterItems;
};

export default useFilterItems;
